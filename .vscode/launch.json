{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run update_shopify_inventory",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/backend/update_shopify_inventory.py",
      "envFile": "${workspaceFolder}/backend/.env"
    }
  ]
}
